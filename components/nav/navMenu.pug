b-navbar.sticky-top.bd-navbar.navbar-light.bg-light(toggleable="md")
  nuxt-link.nav-link(:to="localePath('index')")
    img(src="~/assets/SmarensolLogo.svg", alt="Smaresol", :height="imgHeight")
  b-navbar-toggle(target="nav_collapse")
  b-collapse(is-nav id="nav_collapse")
    b-navbar-nav.ml-auto
      nuxt-link.nav-link(:to="localePath('about-us')") {{ $t('menu.company')}}
      b-nav-item-dropdown(:text="$t('menu.productLines.title')" right)
        b-dropdown-item(to="/product-lines/distribution") {{ $t('menu.productLines.distribution') }}
        b-dropdown-item(to="/product-lines/substations") {{ $t('menu.productLines.substations') }}
        b-dropdown-item(to="/product-lines/transmission-lines") {{ $t('menu.productLines.transmition') }}
        b-dropdown-item(to="/product-lines/industrial") {{ $t('menu.productLines.industrial') }}
      nuxt-link.nav-link(:to="localePath('customers')") {{ $t('menu.customers') }}
      nuxt-link.nav-link(:to="localePath('contact-us')") {{ $t('menu.contactUs') }}
      b-nav-item-dropdown(:text="$i18n.locales.filter(function(i){return i.code == $i18n.locale})[0].name" right)
        nuxt-link.nav-link(
          v-for="locale in $i18n.locales",
          v-if="locale.code !== $i18n.locale",
          :key="locale.code",
          :to="switchLocalePath(locale.code)",
        ) {{ locale.name }}
