# Full project: https://gitlab.com/pages/plain-html

image: node:lts-slim

stages:
  - build
  - test
  - deploy

cache:
  paths:
  - app/node_modules/

build:
  stage: build
  only:
  - master
  script:
  - chown -R node:node .
  - cd app
  - yarn
  - yarn build

test:
  stage: test
  only:
  - master
  script:
  - cd app
  - yarn test

deploy:
  stage: deploy
  only:
  - master
  before_script:
  - apt-get update -y
  # Setup SSH deploy keys
  - 'which ssh-agent || ( apt-get install openssh-client -y)'
  - eval $(ssh-agent -s)
  - mkdir -p ~/.ssh
  - ssh-add <(echo "$SSH_PRIVATE_KEY" | base64 --decode)
  - ssh $SERVER_IP -tt -p 7459
  script:
  - cd app
  - mkdir dist
  - yarn generate
  - scp -P 7459 -r dist/. $SERVER_ADDRESS
  artifacts:
    name: "$CI_JOB_NAME"
    paths:
    - ./app/dist
    expire_in: 2 days
